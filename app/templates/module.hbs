<div class="navbar row">
  <ul id="submodule-nav" class="nav">
    {{#each item in controller}}
      <li>
        {{#link-to item.id }}
          {{item.name}}
        {{/link-to}}
      </li>
    {{/each}}
  </ul>
</div>
{{outlet}}
